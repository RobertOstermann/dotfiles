- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
      path: zshrc
      force: true

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]